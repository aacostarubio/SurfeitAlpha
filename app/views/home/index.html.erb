<h1>INDEX</h1>
 
 <% if session[:user_id] != nil %>
 <% user = User.find_by_id(session[:user_id]).name %>
  
<% else %>
 Don't have an account? <%= link_to "Create an Account", new_user_url%>
 <br>
 <br>
 Are you an <em>awesome</em> Surfeit user? <%= link_to "Sign in", login_url %>
<% end %>

